# 拍照量腳尺寸建議工具 — 開發計畫

> 給開發用的完整規格。目標是純前端靜態網頁，部署在 GitHub Pages，客人用手機拍腳踩 A4 紙的照片，就得到鞋款尺碼建議。
> 本文件是規格與計畫，不含程式碼。開發時依「開發階段」順序做，每階段做完可獨立驗收。

## 實作變更紀錄（2026-09-15）

實際開發時與原計畫不同的地方，以這一節為準：

1. **不用 OpenCV.js，改自寫輕量演算法**。OpenCV.js 約 10 MB，LINE 內建瀏覽器用 4G 要載很久；自寫版本整站不到 100 KB，也能在瀏覽器跑自動測試。
2. **前置說明頁改成 4 張引導卡片**。一張卡片只做一件事（開始、紙放深色地板、紙貼牆腳跟貼牆、手機在腳尖上方拍），取代原本要讀完的長說明。
3. **新增拍完照的問題引導**。偵測失敗或量測有疑慮時，直接說原因和怎麼修，並配「錯 vs 對」示意圖：地板太白、紙角沒入鏡、紙被擋住、找不到紙、找不到腳、腳跟沒貼齊、腳太靠邊、腳尖碰紙邊。
4. **拍攝位置改為手機在腳尖正上方**。手機在腳踝上方時，小腿的投影會蓋住紙靠腳跟那側的兩個角；在腳尖上方時腳尖的視差也最小。
5. **更正第 3 節與第 12 節**：B5 和 A4 長寬比相同（都是 √2），比例擋不掉 B5；Letter 比例約 1.29，反而有機會擋掉。引導卡片強調用 A4 影印紙。
6. **手動標紙角改為拖動四個圓點**，也可以點一下讓最近的圓點移過去，不必依序點四個角。
7. **腳部標記改為四個點**：腳跟、腳尖、兩個腳寬點。腳長是腳跟點到腳尖點的距離，腳寬沿垂直腳軸方向量，腳斜放也能量。
8. **新增「量另一隻腳」**，兩隻腳取較大的數值判斷。
9. **商品改為 DK A0235 夾腳拖兩色**（sku 11802053 黑色、11802051 奶茶色），尺碼 23~28 號。銷售頁沒有腳長對照表，暫定「N 號適合腳長 N-0.5 ~ N+0.5 cm」，`verified: false` 時結果頁會顯示測試中提示。原計畫的 1241068 尺碼表移到 `test/fixtures/` 當尺碼邏輯測試資料。
10. **主色改為 DK 官網紅 #FF5353**。
11. **測試頁**：`test/run.html`（模擬照片 + 尺碼邏輯）、`test/photos.html`（實拍照片準確度與修正值建議）、`test/debug.html`（單張逐步除錯）。

### 2026-09-15 晚上：流程優化

- **開頭改成尺碼對照表**：程式先針對 A0235，一打開就顯示尺碼、適合腳長、適合腳寬，讓客人自己選「拍照量」或「直接輸入」。
- **拿掉人工拖點修正**：拖點在手機上難操作，實測也沒人拖。改成拍完自動量、直接看結果；照片有問題就引導重拍，輕微問題可以「仍然看結果」；結果頁用縮圖呈現量到的範圍。
- **按鈕固定在螢幕底部**：LINE 內建瀏覽器畫面矮，按鈕原本會被擠出畫面。
- **影子提示門檻從 1.3 提高到 2.0**：實拍正常光線 1.55~1.57 會誤報，側光深影是 2.53。
- **尺碼對照表圖片**：`tools/make_size_chart.py` 依尺碼表資料產生商品頁用圖。

### 2026-09-15 傍晚：尺量驗證與尺碼表確認

- 三張辦公室實拍用尺量過，工具量的腳長腳寬正確（27.7×10.5、26.3×10.6、22.3×8.9 cm）。
- 黑襪同事自稱「腳長 27~27.5」其實是平常穿的鞋號，腳長是 26.3。
- A0235 的廠商尺寸 5~10 號對應 DK 標示 23~28 號，尺碼表改用廠商的鞋長、鞋寬、適合腳長，改成已確認，並打開腳寬規則。
- 沒指定商品時，結果頁列出每個商品的建議尺碼與商品連結。
- 試穿回饋：腳長 27.7 cm 穿 28 號「幾乎剛好貼合」，工具原本建議「27 或 28 號」會讓人選到太小的鞋。改成只往大一號建議、不建議小一號，超過最大號範圍直接說沒有適合尺碼。

### 2026-09-15 下午：第一次實拍發現的問題與修正

三個人量出來都是 27.2~27.3 cm。用兩張辦公室實拍追出原因：

- **紙抓不到**：米色地毯本身很亮，用「白度」門檻分不出紙；紙的右半又在腿的影子裡，門檻拉高紙就裂成兩塊。改用「紙張分數」（亮度扣掉色彩範圍）加紋理排除，兩張都能自動抓到。
- **角被腿遮住**：原本把凸包縮成四頂點，被切掉的角會跑到切線上。改用四條最外側支撐邊延長相交，看得到的邊就能還原被遮的角。
- **抓不到紙時的備援形同虛設**：畫面放的是固定位置的預設方框，按下一步不會被擋，三個人都沒拖圓點，量的都是同一個方框。改成使用者要自己點出四個角才能往下走；按下一步時把圓點吸附到紙邊，並檢查四條邊是否真的在紙上，不是就擋（完全不像紙要警告兩次才放行）。
- **影子被算進腳寬**：緊貼腳邊的深影帶地板反射的暖色，跟暗處皮膚很像。改成越暗的地方要求越大的色差才算皮膚，深色門檻從 42% 提高到 70%。照片二腳寬 10.4 → 8.9。
- **確認畫面看不出程式判斷了什麼**：把程式判斷的腳畫成紅色半透明，光線不均時提示「影子不算腳」。
- 修正後：照片二腳長 22.3 cm（與照片像素估算一致）、照片一 27.7 cm。兩張都還沒有尺量的真值。

---

## 1. 專案目標

- 客人在商品頁點連結或掃 QR code，開啟網頁，依前置說明拍照，得到「建議 X 號」的結果。
- 不需要 App、不需要登入、照片不離開手機。
- 尺碼表依品號切換，網址帶 `?sku=1241068` 就載入該款的表。
- 拍照失敗時有備援路徑，流程永遠走得完。

### 非目標（第一版不做）

- 不做帳號、不存照片、不做後端。
- 不做多腳同時量、不做鞋內長換算、不做 3D。
- 不做可列印的定位標記量腳紙（列為未來擴充）。

---

## 2. 使用者流程

```
商品頁連結 ?sku=1241068
  → [0] 前置說明頁（準備 A4 紙、怎麼站、怎麼拍）
  → [1] 拍照 / 上傳照片
  → [2] 自動偵測 A4 四角 → 透視校正
       ├ 成功 → 顯示校正後照片
       └ 失敗 → 請使用者手動點四個角
  → [3] 自動偵測腳趾尖與腳寬 → 顯示標記，使用者可拖動微調
  → [4] 顯示量測結果（腳長 / 腳寬 mm）與尺碼建議
       └ 任何時候都可切換「我自己量好了，直接輸入數字」
```

流程在同一個 `index.html` 內以分步驟畫面切換，不跳頁。

---

## 3. 前置說明頁（畫面文案，直接使用）

說明頁是量測成功率的關鍵，要放在拍照之前，且每一步配一張示意圖。以下是畫面文案。

### 標題

**拍一張照片，幫你選對尺碼**

### 副標

準備一張 A4 白紙和 1 分鐘，照片只在你的手機上處理，不會上傳。

### 步驟 1：準備一張 A4 白紙

- 用一般影印用的 A4 白紙，不要用 B5、信紙或有摺痕的紙。
- 紙要攤平放在**硬地板**上，不要放在地毯或床上。
- 地板要跟白紙有顏色差。白色磁磚、白色地墊會抓不到紙的邊，請改放在深色地板，或在紙下面墊一塊深色布或毛巾。

### 步驟 2：腳跟對齊紙的短邊

- 把紙的短邊靠牆，腳跟貼著牆站上去，腳跟會自然切齊紙邊。
- 沒有牆也可以，用眼睛把腳跟對齊紙的短邊就好。
- 赤腳量最準。穿襪子也可以，但要穿薄襪、貼身的顏色不要是白色。
- 站直，體重平均放在腳上，腳趾自然放鬆，不要抓地。

### 步驟 3：從正上方拍

- 手機拿到腳的正上方，鏡頭朝下，跟地板平行。
- 畫面要拍到**整張紙的四個角**，紙佔畫面越大越好。
- 避開強光直射和反光，也不要讓自己的影子蓋住紙。
- 不要用廣角鏡頭（0.5x），用一般鏡頭（1x）。

### 步驟 4：檢查照片

- 拍完會請你確認紙的四個角和腳趾的位置，抓錯了可以用手指拖動修正。

### 頁尾

- 量出來的是腳長和腳寬，誤差大約在 3 到 5 mm，接近兩個尺碼交界時會同時列出兩個建議。
- 提供「我自己量好了，直接輸入腳長腳寬」的按鈕。
- 主要按鈕：**開始拍照**

### 示意圖需求（4 張，可先用簡單線稿）

1. A4 紙放在深色地板上，旁邊打叉的白色地板。
2. 腳跟貼牆、紙短邊靠牆的側視圖。
3. 手機在正上方水平拍攝的側視圖，畫面內有紙的四個角。
4. 錯誤示範：斜拍、紙角被切掉、影子蓋住紙。

---

## 4. 技術架構

### 原則

- 純靜態：HTML + CSS + 原生 JavaScript，沒有 build step，沒有框架，`git push` 即部署。
- 所有影像處理在瀏覽器端完成，照片不傳出去。
- 必須在 LINE 內建瀏覽器、iOS Safari、Android Chrome 都能跑。

### 關鍵技術決策

| 項目 | 決定 | 理由 |
|------|------|------|
| 拍照 | `<input type="file" accept="image/*" capture="environment">` | LINE 內建瀏覽器常擋 getUserMedia，檔案上傳一定可用 |
| 影像處理 | OpenCV.js，自行放在 repo 內的 `lib/opencv.js` | 找四邊形、透視校正、輪廓都是現成功能；自行託管避免 CDN 失效 |
| 載入策略 | OpenCV.js 約 8 到 10 MB，使用者按「開始拍照」後才載入，顯示進度 | 避免說明頁就卡住 |
| 圖片前處理 | 讀入後長邊縮到 1600 px 再處理 | 手機原圖 4000 px 以上，處理太慢 |
| EXIF 方向 | 用 `createImageBitmap(file, { imageOrientation: "from-image" })`，不支援時退回 exif 解析 | iPhone 直拍照片方向常是轉過的 |
| 水平檢查 | 選配，用 DeviceOrientation；iOS 需在使用者手勢內請求權限，失敗就略過 | LINE 內不一定可用，不能當必要條件 |
| 尺碼表 | `data/sizes/<sku>.json`，用 `?sku=` 載入 | 每款餘量不同，各自維護 |

### Repo 結構

```
foot-size-advisor/
├── index.html              單頁，分步驟畫面
├── css/style.css
├── js/
│   ├── app.js              流程控制、畫面切換
│   ├── image.js            讀檔、EXIF 方向、縮圖
│   ├── paper.js            A4 偵測、透視校正
│   ├── foot.js             腳部分割、腳趾尖、腳寬
│   ├── measure.js          像素轉 mm、信心分數
│   ├── sizing.js           尺碼表查詢與建議邏輯
│   └── ui-adjust.js        四角 / 標記拖動微調
├── lib/opencv.js
├── data/sizes/
│   └── 1241068.json
├── assets/                 示意圖
├── test/
│   ├── photos/             測試照片與人工量測的真值
│   └── sizing.test.html    尺碼邏輯的瀏覽器測試頁
└── README.md
```

---

## 5. 演算法細節

### 5.1 A4 偵測（paper.js）

1. 轉灰階，高斯模糊，Canny 邊緣，膨脹一次把斷線接起來。
2. 找外輪廓，按面積由大到小。
3. 對每個輪廓做多邊形近似，取頂點數為 4、凸多邊形、面積 ≥ 畫面 15% 的第一個。
4. 四點依左上、右上、右下、左下排序。
5. 驗證：四邊比例接近 1:1.414（容許 ±12%），四個角都在畫面內且離邊界 ≥ 5 px。
6. 通過就進入校正；失敗回傳 `null`，由 UI 走手動點四角。

補強：白色地板情境 Canny 抓不到，可以再試一次以「飽和度低且亮度高」做遮罩找輪廓；兩種都失敗才要求手動。

### 5.2 透視校正

- 用四角做 `getPerspectiveTransform`，輸出固定尺寸的校正圖。
- 校正圖比例：短邊 210 mm、長邊 297 mm。用 **5 px = 1 mm**，輸出 1050 × 1485 px。
- 校正後每個像素對應 0.2 mm，之後所有量測都在校正圖上做。
- 紙的方向：以「腳跟對齊的短邊」為底邊。程式判斷方式是校正後腳的輪廓碰到哪一條短邊，那條就是腳跟邊。

### 5.3 腳部分割（foot.js）

在校正圖上：

1. 轉 HSV。紙是「高亮度、低飽和度」，腳和襪子不是。做遮罩：`S > 40` 或 `V < 150` 的像素視為非紙。門檻要能從測試照片調整，寫成常數。
2. 形態學：開運算去雜點，閉運算補洞。
3. 找輪廓，取**面積最大且碰到腳跟邊**的那個。沒有碰到任何短邊就取最大的，並把信心分數降低。
4. 過濾陰影：陰影通常飽和度低、亮度中等。若分割結果寬度異常（> 140 mm）就把亮度門檻收緊再做一次。

### 5.4 腳長與腳寬（measure.js）

- **腳長** = 腳跟邊到輪廓最遠點的距離（沿長邊方向）。腳跟對齊紙邊時，這就是腳長。
- **腳寬** = 在腳長前 55% 到 75% 的區段（腳掌最寬處，蹠骨球），取輪廓左右邊界差的最大值。不要取全輪廓最大寬，會被腳踝或小腿影響。
- 若使用者選了「腳跟沒對齊紙邊」模式，腳長改為輪廓沿長邊的最遠兩點距離，並強制進入手動確認。
- 換算：像素 × 0.2 = mm。輸出取到 1 mm。

### 5.5 信心分數

回傳 0 到 1 的分數，用來決定 UI 要不要強制使用者確認：

- 四角自動偵測成功 +0.3，手動點角 +0.2
- 腳輪廓碰到腳跟邊 +0.3
- 腳寬在 70 到 130 mm 之間 +0.2
- 腳長在 200 到 300 mm 之間 +0.2

分數 < 0.7 時，標記拖動畫面顯示「請確認腳趾尖和腳寬位置」的醒目提示；≥ 0.7 也顯示標記，但提示文字較弱。**任何情況都讓使用者能拖動。**

---

## 6. 尺碼建議邏輯（sizing.js）

### 6.1 尺碼表 JSON 格式

`data/sizes/1241068.json`：

```json
{
  "sku": "1241068",
  "name": "範例拖鞋",
  "unit": "mm",
  "fit_note": "鞋長比腳長多約 20 mm，屬正常餘量",
  "width_rule": {
    "enabled": true,
    "margin_mm": 0,
    "note": "腳寬 ≥ 該號鞋寬 + margin 時建議加一號"
  },
  "sizes": [
    { "label": "5號",  "shoe_length": 245, "shoe_width": 98,  "foot_min": 225, "foot_max": 235 },
    { "label": "6號",  "shoe_length": 255, "shoe_width": 98,  "foot_min": 235, "foot_max": 245 },
    { "label": "7號",  "shoe_length": 265, "shoe_width": 103, "foot_min": 245, "foot_max": 255 },
    { "label": "8號",  "shoe_length": 275, "shoe_width": 103, "foot_min": 255, "foot_max": 265 },
    { "label": "9號",  "shoe_length": 285, "shoe_width": 108, "foot_min": 265, "foot_max": 275 },
    { "label": "10號", "shoe_length": 295, "shoe_width": 110, "foot_min": 275, "foot_max": 285 }
  ]
}
```

數值來源是商品頁的尺碼對照表。之後每個品號各一份。

### 6.2 判斷步驟

1. 用腳長找落在 `foot_min ≤ 腳長 < foot_max` 的尺碼，作為 **主建議**。
2. **邊界判斷**：腳長離 `foot_min` 或 `foot_max` 在 3 mm 以內，同時列出相鄰尺碼，文案寫「X 號或 Y 號都可以」。
3. **腳寬修正**：`width_rule.enabled` 時，若腳寬 ≥ 主建議尺碼的 `shoe_width + margin_mm`，建議加一號，文案寫「腳掌偏寬，建議選 Y 號」。
4. 腳長小於最小尺碼或大於最大尺碼：顯示「這款沒有適合的尺碼」，並列出最接近的一號與提醒。
5. 結果畫面同時顯示：量到的腳長腳寬、建議尺碼、該尺碼的鞋長鞋寬、量測誤差提醒。

### 6.3 腳寬規則要用實測資料校正

商品頁的試穿紀錄顯示：腳長 23.0、腳寬 9.8 的人選了 7 號，單看腳長會給 5 號。這代表對寬腳而言，腳長規則會嚴重低估。`margin_mm` 與 `width_rule` 要用店家提供的試穿紀錄逐款調整，第一版先用 0，並在 README 記錄調整方式。

---

## 7. 備援與錯誤處理

| 情境 | 處理 |
|------|------|
| 找不到 A4 | 顯示原圖，請使用者依序點左上、右上、右下、左下四個角，點完可拖動微調 |
| 手動四角比例明顯不對（偏離 1.414 超過 20%） | 提示「這好像不是 A4，請確認」，但仍允許繼續 |
| 分割不到腳 | 校正圖上放預設標記（腳趾尖在中線、腳寬在中段），請使用者拖到正確位置 |
| 腳長超出 300 mm 或低於 150 mm | 提示可能是紙抓錯或不是 A4，回到四角確認 |
| 照片載入失敗、格式不支援 | 提示重新拍，並提供手動輸入入口 |
| OpenCV.js 載入失敗 | 直接切到手動輸入模式，不要卡住 |
| 找不到 `?sku=` 或 JSON 不存在 | 顯示量測結果但不給尺碼，提示「請回商品頁對照尺碼表」 |

手動輸入模式：兩個數字欄位（腳長、腳寬，cm），直接進入尺碼判斷。

---

## 8. 畫面規格

- 手機優先，寬度 375 px 為基準，桌機置中最大 480 px。
- 每個步驟一個畫面，上方有進度指示（說明 → 拍照 → 確認紙 → 確認腳 → 結果）。
- 拖動標記：圓形手把直徑 ≥ 44 px，方便手指操作；拖動時上方顯示放大鏡預覽。
- 結果畫面有「重新量」和「回商品頁」按鈕。回商品頁的網址由 `?back=` 參數帶入，沒有就隱藏。
- 顏色風格跟品牌商品頁一致，最終以品牌提供的色碼為準（實作採 DK 官網紅 `#FF5353`）。
- 所有文案繁體中文，不用簡體用語。

---

## 9. 開發階段

每階段獨立可驗收，做完再進下一階段。

### Phase 0：骨架與說明頁（半天）

- repo 建立、GitHub Pages 啟用、`index.html` 分步驟畫面切換。
- 前置說明頁完整文案與示意圖占位。
- 手動輸入模式 + 尺碼判斷邏輯 + `1241068.json`。
- 驗收：不拍照也能從說明頁走到結果頁，尺碼判斷對照表格正確。

### Phase 1：拍照與 A4 校正（1 天）

- 檔案上傳、EXIF 方向、縮圖。
- A4 自動偵測、透視校正、手動點四角備援。
- 驗收：測試照片集裡 80% 以上自動抓到紙，其餘手動點角都能校正成功。

### Phase 2：腳部量測（1 到 2 天）

- 分割、腳趾尖、腳寬、信心分數。
- 標記拖動微調畫面。
- 驗收：測試照片集裡，人工用尺量的真值與程式量測差距，90% 在 ±4 mm 內。

### Phase 3：結果與細節（半天）

- 結果畫面、邊界文案、腳寬修正、錯誤處理全部接上。
- LINE 內建瀏覽器、iOS Safari、Android Chrome 各實測一輪。
- 驗收：所有第 7 節的情境都有對應畫面，流程沒有死路。

### Phase 4：上線（半天）

- 商品頁「詳細說明」放連結與 QR code。
- README 寫清楚如何新增品號 JSON、如何調 `width_rule`。

---

## 10. 測試計畫

### 測試照片集

開發前先蒐集至少 20 張照片，放在 `test/photos/`，每張附一個同名 JSON 記錄人工用尺量的腳長腳寬真值。要涵蓋：

- 深色地板、木地板、白色磁磚（預期失敗）
- 赤腳、深色薄襪、白襪（預期分割困難）
- 正上方拍、輕微斜拍（15 度）、明顯斜拍（30 度，預期誤差變大）
- 腳跟對齊紙邊、腳跟沒對齊
- 室內燈光、窗邊自然光、有影子

### 驗收指標

| 指標 | 目標 |
|------|------|
| A4 自動偵測成功率（排除白色地板） | ≥ 80% |
| 腳長誤差 ±4 mm 內比例（正上方拍） | ≥ 90% |
| 腳寬誤差 ±5 mm 內比例 | ≥ 80% |
| 手動備援後流程完成率 | 100% |
| 說明頁到結果頁所需時間 | ≤ 2 分鐘 |

### 尺碼邏輯測試

`test/sizing.test.html` 用瀏覽器跑，覆蓋：每個區間中點、每個邊界值 ±3 mm、腳寬觸發加一號、超出範圍。

---

## 11. 部署

- GitHub repo 設定 Pages，來源 `main` 分支根目錄。
- 網址格式：`https://<帳號>.github.io/foot-size-advisor/?sku=1241068&back=<商品頁網址>`
- 若之後要綁自訂網域，加 `CNAME` 檔即可。
- `lib/opencv.js` 約 10 MB，在 GitHub 單檔上限 100 MB 內，直接進 repo。

---

## 12. 已知限制（要寫進說明頁或 README）

- 程式無法辨識紙是不是真的 A4。B5 可以用比例擋掉，Letter 比例太接近擋不掉。
- 斜拍時腳背會投影超出腳趾尖，腳長會量大。正上方拍加校正可以壓到 ±4 mm。
- 尺碼表上的「鞋寬」是鞋外寬，不等於腳寬，腳寬規則必須用試穿紀錄校正。
- 拖鞋這類鞋款的尺碼餘量與包鞋不同，每款 JSON 要各自維護。

---

## 13. 未來擴充

- 可列印的量腳紙，四角印 ArUco 定位標記，偵測率接近 100%，並可直接印上腳跟對齊線。
- 多品號一次比對：量一次腳，列出多款各自的建議尺碼。
- 收集匿名的量測數值（不含照片）回饋給店家調整 `width_rule`。
- 用輕量自製演算法取代 OpenCV.js，把載入量從 10 MB 壓到 100 KB 以下。

---

## 14. 給開發者的注意事項

- 不引入框架、不用 npm build。所有檔案都是瀏覽器直接讀的靜態檔。
- 所有門檻值（Canny、HSV 遮罩、比例容許、邊界 3 mm）集中放在 `js/config.js`，不要散在各檔案。
- 每個影像處理步驟都要能在開發模式下把中間結果畫到畫面上（`?debug=1`），否則調參數會很痛苦。
- OpenCV.js 的 `Mat` 用完一定要 `delete()`，否則手機記憶體很快爆。
- 拖動標記的座標系統要跟校正圖一致，顯示時再縮放，不要在顯示座標上量測。
- 文案不要寫「精準」「準確」這類字，寫「建議」「大約」，並且永遠顯示誤差提醒。
